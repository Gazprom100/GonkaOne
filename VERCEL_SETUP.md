# Настройка деплоя на Vercel

## ✅ Текущий статус

Проект успешно подключен к Vercel:
- **Проект**: `kconsulting/client`
- **Preview URL**: https://client-8acdh4dnd-kconsulting.vercel.app
- **Production URL**: будет доступен после завершения деплоя

## 🔧 Настройка переменных окружения

1. Перейдите в панель Vercel: https://vercel.com/kconsulting/client
2. Откройте **Settings** → **Environment Variables**
3. Добавьте следующие переменные:

```
REACT_APP_API_URL=https://your-backend-url.com/api
```

**Важно**: Backend нужно деплоить отдельно на Render.com или другом сервисе.

## 🚀 Деплой Backend на Render.com

Так как Vercel лучше подходит для frontend, backend рекомендуется деплоить на Render:

1. Создайте аккаунт на https://render.com
2. Подключите GitHub репозиторий
3. Создайте новый **Web Service**
4. Настройки:
   - **Build Command**: `npm install && cd client && npm install && npm run build`
   - **Start Command**: `npm start`
   - **Environment**: Node
5. Добавьте переменные окружения из `.env.example`
6. После деплоя получите URL backend и обновите `REACT_APP_API_URL` в Vercel

## 📝 Команды для деплоя

### Frontend (Vercel)
```bash
cd client
vercel --prod
```

### Или через Git (автоматический деплой)
```bash
git push origin main
# Vercel автоматически задеплоит при пуше в main ветку
```

## 🔗 Настройка домена

1. В панели Vercel перейдите в **Settings** → **Domains**
2. Добавьте свой домен
3. Настройте DNS записи согласно инструкциям Vercel

## ⚙️ Настройка Telegram WebApp

После получения production URL:

1. Откройте @BotFather в Telegram
2. Выполните команду `/newapp` или `/myapps`
3. Выберите вашего бота
4. Укажите:
   - **Title**: GonkaOne
   - **Description**: Коллективный майнинг-пул
   - **Photo**: (опционально)
   - **Web App URL**: `https://your-vercel-domain.vercel.app`
5. Сохраните изменения

## 🔄 Обновление после изменений

После любых изменений в коде:

```bash
# Локальная проверка
cd client
npm run build

# Деплой на Vercel
vercel --prod
```

Или просто сделайте `git push` если настроен автоматический деплой.

## 📊 Мониторинг

- **Логи**: https://vercel.com/kconsulting/client/logs
- **Аналитика**: https://vercel.com/kconsulting/client/analytics
- **Deployments**: https://vercel.com/kconsulting/client/deployments

## 🐛 Решение проблем

### Ошибка сборки
- Проверьте логи в панели Vercel
- Убедитесь что все зависимости установлены
- Проверьте версию Node.js (должна быть 18+)

### API не работает
- Убедитесь что backend задеплоен и доступен
- Проверьте `REACT_APP_API_URL` в переменных окружения
- Проверьте CORS настройки на backend

### Telegram WebApp не открывается
- Проверьте что URL правильный
- Убедитесь что бот настроен в BotFather
- Проверьте что приложение доступно по HTTPS

## 📞 Поддержка

- Vercel Docs: https://vercel.com/docs
- Vercel Support: https://vercel.com/support

