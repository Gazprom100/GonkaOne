# 🚀 Полная настройка Telegram бота и WebApp

## ✅ Текущий статус

- ✅ Vercel подключен
- ✅ Приложение задеплоено: https://gonkaone.vercel.app
- ✅ TELEGRAM_BOT_TOKEN установлен в Vercel
- ✅ Создан обработчик webhook для бота

## 📱 Шаг 1: Настройка WebApp в @BotFather

### 1.1 Создайте WebApp

1. Откройте **@BotFather** в Telegram
2. Отправьте: `/newapp`
3. Выберите вашего бота
4. Заполните:
   - **Title**: `GonkaOne`
   - **Description**: `Коллективный майнинг-пул`
   - **Web App URL**: `https://gonkaone.vercel.app`
   - **Short name**: `gonkaone`
5. Сохраните

### 1.2 Настройте кнопку меню (ОБЯЗАТЕЛЬНО!)

1. В @BotFather отправьте: `/setmenubutton`
2. Выберите вашего бота
3. Настройте:
   - **Text**: `🚀 Открыть`
   - **Web App**: выберите созданное WebApp
4. Сохраните

## 🔧 Шаг 2: Настройка Webhook

Webhook нужен чтобы бот отвечал на команды и открывал WebApp через кнопки.

### Вариант A: Автоматическая настройка (рекомендуется)

```bash
cd /Users/evgenikrasnov/Desktop/GitHub/GonkaOne
./scripts/setup-telegram-webhook.sh
```

### Вариант B: Ручная настройка

1. Получите токен из Vercel:
```bash
vercel env pull .env.local
grep TELEGRAM_BOT_TOKEN .env.local
```

2. Установите webhook:
```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://gonkaone.vercel.app/api/telegram/webhook"}'
```

3. Проверьте webhook:
```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

### Вариант C: Через Vercel Function

Откройте в браузере:
```
https://gonkaone.vercel.app/api/telegram/setwebhook
```

Или отправьте POST запрос:
```bash
curl -X POST https://gonkaone.vercel.app/api/telegram/setwebhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://gonkaone.vercel.app/api/telegram/webhook"}'
```

## 🎯 Шаг 3: Проверка работы

### 3.1 Проверка WebApp

1. Откройте чат с вашим ботом в Telegram
2. **Внизу экрана должна быть кнопка** с текстом "🚀 Открыть"
3. Нажмите на кнопку - откроется WebApp

### 3.2 Проверка команд бота

Отправьте боту команды:

- `/start` - Бот должен ответить с кнопкой для открытия WebApp
- `/help` - Бот должен показать справку
- `/invite` - Бот должен показать реферальную ссылку

## 🔍 Шаг 4: Диагностика проблем

### Проблема: WebApp не открывается

**Решение:**
1. Проверьте что WebApp создан: `/myapps` в @BotFather
2. Проверьте что кнопка меню настроена: `/setmenubutton` в @BotFather
3. Проверьте URL: `https://gonkaone.vercel.app`
4. Перезапустите Telegram

### Проблема: Бот не отвечает на команды

**Решение:**
1. Проверьте что webhook установлен:
```bash
curl "https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo"
```

2. Проверьте логи Vercel:
   - Откройте: https://vercel.com/kconsulting/gonkaone/logs
   - Найдите ошибки в логах

3. Переустановите webhook:
```bash
./scripts/setup-telegram-webhook.sh
```

### Проблема: Webhook не работает

**Решение:**
1. Убедитесь что функция задеплоена:
   - Проверьте: https://gonkaone.vercel.app/api/telegram/webhook
   - Должен вернуть ошибку метода (это нормально для GET)

2. Проверьте переменные окружения в Vercel:
   - `TELEGRAM_BOT_TOKEN` должен быть установлен
   - Проверьте через: `vercel env ls`

3. Перезадеплойте проект:
```bash
vercel --prod
```

## 📋 Чеклист настройки

- [ ] WebApp создан в @BotFather
- [ ] Кнопка меню настроена через `/setmenubutton`
- [ ] Webhook установлен и работает
- [ ] Бот отвечает на команды `/start`, `/help`, `/invite`
- [ ] WebApp открывается через кнопку меню
- [ ] WebApp открывается через команду `/start`

## 🎉 Готово!

После выполнения всех шагов:

1. **WebApp доступен** через кнопку меню в боте
2. **Бот отвечает** на команды и открывает WebApp через кнопки
3. **Реферальные ссылки** работают через параметр `/start CODE`

## 📞 Полезные команды

### Проверка webhook
```bash
curl "https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo"
```

### Удаление webhook (если нужно)
```bash
curl -X POST "https://api.telegram.org/bot<ТОКЕН>/deleteWebhook"
```

### Проверка бота
```bash
curl "https://api.telegram.org/bot<ТОКЕН>/getMe"
```

### Логи Vercel
Откройте: https://vercel.com/kconsulting/gonkaone/logs

