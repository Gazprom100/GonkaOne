# GonkaOne - Коллективный майнинг-пул с реферальной программой

Веб-приложение для коллективного майнинг-пула токенов GNK с ультра современным неоновым дизайном.

## 🚀 Особенности

- **Майнинг-пулы**: Инвестирование в коллективные пулы для добычи токенов GNK
- **Реферальная программа**: Многоуровневая система (3 уровня: 5%, 3%, 2%)
- **Управление кошельками**: Поддержка Gonka и BEP-20 кошельков
- **Вывод средств**: Запросы на вывод USDT (BEP-20)
- **Telegram интеграция**: Авторизация через Telegram WebApp
- **Неоновый дизайн**: Ультра современный криптовый стиль с эффектами свечения

## 📋 Требования

- Node.js 16+
- npm или yarn

## 🛠 Установка

1. Клонируйте репозиторий
2. Установите зависимости:

```bash
npm run install-all
```

3. Настройте переменные окружения:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл и укажите:
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `TELEGRAM_BOT_USERNAME` - username бота
- `JWT_SECRET` - секретный ключ для JWT
- `DB_PATH` - путь к базе данных

4. Запустите приложение:

```bash
# Development режим (frontend + backend)
npm run dev

# Или отдельно:
npm run server  # Backend на порту 3000
npm run client  # Frontend на порту 3001
```

## 📁 Структура проекта

```
GonkaOne/
├── client/                 # React frontend
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы приложения
│   │   ├── context/       # React Context (Auth)
│   │   └── ...
│   └── package.json
├── routes/                 # API routes
├── models/                # Модели данных
├── config/                # Конфигурация (БД)
├── middleware/            # Express middleware
├── server.js              # Главный серверный файл
└── package.json
```

## 🔧 API Endpoints

### Авторизация
- `POST /api/auth/telegram` - Авторизация через Telegram
- `GET /api/auth/verify` - Проверка токена

### Пользователи
- `GET /api/users/profile` - Получить профиль
- `PUT /api/users/profile` - Обновить профиль

### Пулы
- `GET /api/pools` - Список всех пулов
- `GET /api/pools/:id` - Получить пул по ID
- `GET /api/pools/my/investments` - Мои инвестиции
- `POST /api/pools/:id/invest` - Инвестировать в пул

### Рефералы
- `GET /api/referrals/stats` - Статистика рефералов
- `GET /api/referrals/level/:level` - Рефералы по уровню
- `POST /api/referrals/register` - Регистрация по реферальной ссылке

### Кошельки
- `GET /api/wallets` - Получить кошельки
- `PUT /api/wallets/gonka` - Обновить Gonka кошелек
- `PUT /api/wallets/bep20` - Обновить BEP-20 кошелек

### Выводы
- `GET /api/withdrawals` - Список запросов на вывод
- `POST /api/withdrawals` - Создать запрос на вывод

## 🎨 Дизайн

Приложение использует ультра современную неоновую цветовую палитру:
- **Неоновый циан** (#00FFFF) - основной акцент
- **Неоновый фиолетовый** (#BF00FF) - вторичный акцент
- **Неоновый розовый** (#FF00FF) - дополнительные элементы
- **Неоновый зеленый** (#00FF41) - успех/положительные значения
- **Темный фон** (#000000, #0A0A0A) - базовые цвета

## 📱 Telegram WebApp

Приложение интегрировано с Telegram WebApp для:
- Авторизации через Telegram
- Получения данных пользователя
- Реферальных ссылок через бота

## 🚢 Деплой

### Render.com
1. Подключите GitHub репозиторий
2. Укажите команду сборки: `npm run build`
3. Укажите команду запуска: `npm start`
4. Добавьте переменные окружения из `.env`

### Vercel/Netlify
1. Установите зависимости: `npm install`
2. Соберите frontend: `cd client && npm run build`
3. Настройте переменные окружения
4. Деплой

## 📝 Лицензия

MIT

## 👥 Авторы

GonkaOne Team

