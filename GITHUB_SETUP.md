# Инструкция по отправке в GitHub и деплою на Vercel

## ✅ Текущий статус

- ✅ Git репозиторий инициализирован
- ✅ Все файлы добавлены и закоммичены
- ⏳ Ожидается создание репозитория на GitHub

## 📤 Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Заполните:
   - **Repository name**: `GonkaOne` (или другое название)
   - **Description**: GonkaOne - Collective mining pool platform with referral program
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
3. Нажмите **Create repository**

## 🔗 Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
cd /Users/evgenikrasnov/Desktop/GitHub/GonkaOne

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/GonkaOne.git

# Или если используете SSH:
git remote add origin git@github.com:YOUR_USERNAME/GonkaOne.git

# Отправьте код
git push -u origin main
```

## 🚀 Шаг 3: Подключите Vercel к GitHub

1. Перейдите на https://vercel.com/new
2. Нажмите **Import Git Repository**
3. Выберите **GitHub** и авторизуйтесь
4. Найдите ваш репозиторий **GonkaOne** и нажмите **Import**
5. Настройки проекта:
   - **Framework Preset**: Other
   - **Root Directory**: `./` (корень проекта)
   - **Build Command**: `cd client && npm install && npm run build`
   - **Output Directory**: `client/build`
   - **Install Command**: `cd client && npm install`
6. Добавьте переменные окружения:
   - `REACT_APP_API_URL` = `https://your-backend-url.com/api` (после деплоя backend)
7. Нажмите **Deploy**

## ⚙️ Альтернативный способ через CLI

Если хотите подключить через CLI:

```bash
# Убедитесь что код в GitHub
git push -u origin main

# Отвяжите старый проект (если был)
cd /Users/evgenikrasnov/Desktop/GitHub/GonkaOne
rm -rf .vercel

# Подключите к новому проекту
vercel link

# Выберите:
# - Set up and deploy? Yes
# - Which scope? Ваш аккаунт
# - Link to existing project? No
# - Project name? gonkaone (или другое)
# - Directory? ./
# - Override settings? No

# Деплой
vercel --prod
```

## 📝 Настройка после деплоя

После успешного деплоя:

1. **Получите URL приложения** из панели Vercel
2. **Настройте переменные окружения**:
   - `REACT_APP_API_URL` - URL вашего backend API
3. **Деплойте backend** на Render.com (используйте `render.yaml`)
4. **Настройте Telegram WebApp** в @BotFather с URL от Vercel

## 🔄 Автоматический деплой

После подключения к GitHub, Vercel будет автоматически деплоить при каждом push в main ветку!

## ❓ Проблемы?

Если возникли проблемы:
- Проверьте что все файлы закоммичены: `git status`
- Убедитесь что remote добавлен: `git remote -v`
- Проверьте что код запушен: `git log --oneline`

